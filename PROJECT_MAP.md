# خريطة مشروع GE_Sky

> آخر تحديث: 07 سبتمبر 2026
>
> الحالة: **نسخة تأسيسية قابلة للتشغيل — واجهة لوحة التحكم العربية RTL**

## [TECH_STACK]

| الطبقة | الاختيار الحالي | الملاحظة |
|---|---|---|
| الواجهة | React 19 + TypeScript + Vite | واجهة تفاعلية في صفحة واحدة مع تقسيم منطقي للوحدات |
| التصميم | Tailwind CSS 4 + CSS مخصص | رموز تصميم مستوحاة من المواصفة: كحلي، أزرق سماوي، أخضر نجاح |
| المكونات | Lucide React + مكونات Radix/shadcn الموجودة في القالب | قابلية وصول وتوسعة تدريجية |
| الحالة | React state محلي للنسخة الأولى | سينتقل إلى TanStack Query عند ربط الإجراءات بالـ API |
| الخادم | Express + tRPC 11 في قالب WebDev | المصادقة عبر Manus OAuth جاهزة في القالب |
| قاعدة البيانات | Drizzle ORM + MySQL/TiDB | مخطط المصادقة موجود؛ مخطط المجال قيد المرحلة التالية |
| التخزين | S3-compatible storage عبر القالب | لا توجد أصول كبيرة محلية في `client/public` |
| التحقق | TypeScript و Vite production build و Vitest | نتائج التحقق تُحدّث بعد كل مرحلة مؤثرة |

## [SYSTEM_FLOW]

1. يفتح المستخدم مساحة العمل في `/`.
2. يختار وحدة من الشريط الجانبي: النظرة العامة، المنتجات، العملاء، الحملات، التحليلات، أو الوكلاء.
3. تعرض الواجهة سياق الوحدة ومؤشرات الأداء والبيانات التشغيلية ذات الصلة.
4. يمكن للمستخدم تنفيذ إجراءات آمنة محلياً في النسخة الحالية: البحث في المنتجات، إضافة منتج كمسودة، وتبديل حالة الحملة.
5. تعرض لوحة النظرة العامة مؤشرات موحدة، اتجاه المبيعات، توزيع القنوات، توصية AI، وصحة العمليات.
6. عند ربط طبقة البيانات، يُستبدل مصدر البيانات التجريبي بإجراءات tRPC محمية بنطاق مساحة العمل، مع إبقاء نفس مسارات الاستخدام البصرية.

## [ARCHITECTURE]

### حدود النسخة الأولى

النسخة الحالية هي **Vertical Slice بصري وتشغيلي** يثبت رحلة المستخدم الرئيسية وهوية المنتج، دون ادعاء جاهزية الإنتاج للبيانات التجارية الحقيقية. تم اختيار هذا الحد لأن المواصفة شاملة جداً، ولأن بناء كل التكاملات الخارجية وقواعد العزل متعدد المستأجرين في دفعة واحدة يزيد المخاطر ويمنع التحقق المرحلي.

### تقسيم الواجهة

- `client/src/pages/Home.tsx`: الحاوية الرئيسية، التنقل، وصفحات الوحدات الأساسية.
- `client/src/index.css`: رموز التصميم، RTL، الاستجابة، الحركة، والجداول.
- `client/index.html`: لغة الصفحة، الاتجاه، بيانات SEO الأساسية.
- مكونات القالب الموجودة في `client/src/components/` تبقى قابلة لإعادة الاستخدام في المراحل اللاحقة.

### حدود الخادم المستقبلية

سيُضاف كل مجال كوحدة مستقلة صغيرة: `tenants`, `products`, `customers`, `campaigns`, `analytics`, `agents`, ثم `billing` و`integrations`. كل إجراء مجال سيُنفذ من خلال tRPC، مع سياق مساحة العمل، التحقق، السجل التدقيقي، وحدود الصلاحيات.

### قرار البساطة

لم تُضف Redis أو محرك بحث أو Elasticsearch أو Queue Worker في هذه المرحلة؛ لا توجد حاجة تحققها النسخة التأسيسية. ستُضاف فقط عند وجود تدفق مستخدم قابل للقياس يبررها.

## [MILESTONES]

| المرحلة | هدف قابل للتحقق | الحالة |
|---|---|---|
| 1. التأسيس | فتح التطبيق بواجهة عربية RTL وهوية GE_Sky واضحة | مكتملة |
| 2. مركز القيادة | رؤية مؤشرات المبيعات والقنوات والعمليات والنشاط | مكتملة |
| 3. المنتجات | البحث، الإحصاءات، جدول الكتالوج، إضافة مسودة | مكتملة |
| 4. CRM | عرض قاعدة العملاء، القيمة الدائمة، والشرائح | مكتملة |
| 5. الحملات | عرض الحملات، الإنفاق، ROAS، وتفعيل/تعطيل الحالة | مكتملة |
| 6. التحليلات والوكلاء | عرض مؤشرات النمو وأدوار الوكلاء | مكتملة |
| 7. البيانات الحقيقية | مخطط المجال، إجراءات tRPC، migrations، RLS/عزل مساحة العمل | قيد الانتظار |
| 8. المصادقة والصلاحيات | حماية الوحدات والأدوار وسجل التدقيق | القالب جاهز، ربط المجال قيد الانتظار |
| 9. التكاملات | S3، البريد/SMS، الإعلانات، القنوات الاجتماعية، الفوترة | قيد الانتظار |
| 10. الإطلاق | اختبارات E2E، مراقبة، نسخ احتياطية، نشر ومراجعة أمنية | قيد الانتظار |

## [ORPHANS & PENDING]

- ربط المنتجات والعملاء والحملات والتحليلات بجداول Drizzle وإجراءات tRPC.
- إضافة `tenant_id` إلى جداول المجال وتطبيق عزل مساحة العمل على كل استعلام.
- استبدال البيانات التجريبية بمصدر بيانات حقيقي وحالات loading/empty/error.
- إضافة اختبارات وحدات وإجراءات للمنتجات والعملاء والحملات، ثم اختبارات رحلة المتصفح.
- ربط وكيل الذكاء الاصطناعي بخدمة LLM بعد تحديد النموذج والميزانية وحدود الاستخدام.
- إضافة إدارة المتغيرات، المخزون، الطلبات، الشحنات، والإرجاع.
- إضافة إدارة الأدوار الدقيقة، 2FA، سجل التدقيق، والفوترة.
- مراجعة الوصولية، الأداء، الأمن، وسياسة الاحتفاظ بالبيانات قبل الإنتاج.

## [ASSUMPTIONS]

- يبدأ المنتج كلوحة تشغيل داخلية باللغة العربية، مع قابلية إضافة الإنجليزية والفرنسية لاحقاً.
- العملة الافتراضية هي الدينار الجزائري، والمنطقة الزمنية المعروضة هي منطقة المستخدم.
- النسخة التأسيسية لا تنفذ مدفوعات أو رسائل أو إعلانات خارجية؛ لا توجد مفاتيح خارجية مطلوبة.
- البيانات الظاهرة تجريبية لغرض التحقق من الرحلة البصرية فقط.

## [VERIFICATION]

- فحص TypeScript وبناء الإنتاج: يُسجلان بعد اكتمال التنفيذ الحالي.
- التحقق البصري: مطلوب عبر معاينة المشروع للعرض المكتبي والجوال.
- اختبار التفاعل: التنقل، البحث في المنتجات، إضافة مسودة، وتبديل الحملة.

## [UPDATE — 07 سبتمبر 2026]

- تمت إضافة تصفية متقدمة للكتالوج حسب التصنيف والحالة ونطاق السعر والتوفر في المخزون.
- تمت إضافة فرز حسب الأحدث، الاسم، السعر، والمخزون مع عداد للفلاتر النشطة.
- تمت إضافة تحميل هيكلي سلس أثناء إعادة حساب النتائج وحالة فارغة واضحة عند عدم وجود تطابق.
- تمت إضافة رسالة نجاح عند إنشاء مسودة منتج ورسالة خطأ عند محاولة الحفظ دون اسم.
- تمت إضافة تصدير تقارير التحليلات بصيغة XLS لفتحها مباشرة في Excel، وفتح نسخة HTML للطباعة والحفظ كـ PDF.
